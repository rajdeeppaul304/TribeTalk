import { useState } from "react"
import { Link, useNavigate } from "react-router-dom"
import { AtSign, LockKeyhole, Mail, MessageCircle } from "lucide-react"
import { useRegisterMutation } from "../features/auth/auth.api"
export default function Register(){const [username,setUsername]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [confirmPassword,setConfirmPassword]=useState("");const [error,setError]=useState("");const navigate=useNavigate();const [register,{isLoading}]=useRegisterMutation();const submit=async(e:React.FormEvent)=>{e.preventDefault();if(password!==confirmPassword)return setError("Passwords do not match.");try{await register({username,email,password}).unwrap();navigate("/login",{replace:true})}catch{setError("We could not create that account.")}};const fields=[{value:username,set:setUsername,placeholder:"Enter your username",type:"text",icon:AtSign},{value:email,set:setEmail,placeholder:"Enter your email",type:"email",icon:Mail},{value:password,set:setPassword,placeholder:"Enter your password",type:"password",icon:LockKeyhole},{value:confirmPassword,set:setConfirmPassword,placeholder:"Confirm your password",type:"password",icon:LockKeyhole}];return <main className="app-shell flex min-h-screen items-center justify-center p-5"><section className="surface w-full max-w-md rounded-2xl p-8 sm:p-10"><div className="flex justify-center"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-accent text-ink"><MessageCircle/></div></div><h1 className="mt-5 text-center text-2xl font-semibold">Create your TribeTalk</h1><p className="mt-1 text-center text-sm text-[#aeb9b8]">Start your next community conversation.</p><form onSubmit={submit} className="mt-8 space-y-3">{fields.map(({value,set,placeholder,type,icon:Icon})=><label key={placeholder} className="relative block"><Icon className="absolute left-3 top-3 text-[#899493]" size={18}/><input className="field pl-10" value={value} onChange={e=>set(e.target.value)} placeholder={placeholder} type={type} required/></label>)}{error&&<p role="alert" className="text-sm text-red-300">{error}</p>}<button className="primary-button mt-2 w-full" disabled={isLoading}>{isLoading?"Creating account…":"Create account"}</button></form><p className="mt-7 text-center text-sm text-[#aeb9b8]">Already here? <Link className="text-accent hover:text-accent-soft" to="/login">Log in</Link></p></section></main>}
